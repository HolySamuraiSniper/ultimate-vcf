---
description: Ultimate VCF Phase 4 — build one slice; verifier gates acceptance
---

Invoke the `ultimate-vcf:uvcf-build` skill. Red→Green then PROVE: a slice that fails its own checks does not advance.
