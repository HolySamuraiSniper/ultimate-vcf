# <slug> slice <N> — Self-Eval (BUILD Phase 4)

> Scored by the builder at the slice boundary, BEFORE handing off to CONFIRM. Any axis < 5 MUST cite concrete evidence and get a fix in this phase — the slice does not advance until it does. (agent-self-evaluation + verifier-gates-acceptance.)

| Axis | Score 1-5 | Evidence (required if < 5) |
|------|-----------|----------------------------|
| Accuracy — does it do what the criterion says? | | |
| Completeness — all acceptance criteria for this slice met? | | |
| Clarity — would another dev understand it cold? | | |
| Surgical-ness — only touched what the slice needs? | | |
| No-placeholders — no `// ...`, no half-finished impls? | | |

**Proof commands run (paste actual output, not "should pass"):**

```
<tests / typecheck / lint / eval output>
```

**Top 1-3 fixes applied before handoff:**
1.
